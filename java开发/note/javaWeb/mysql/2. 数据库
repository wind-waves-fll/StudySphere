数据库：存储数据的仓库
    英文：DataBase,简称DB
数据库管理系统：
    管理数据库的大型软件
    英文：DataBase Management System,简称DBMS
SQL:
    英文：Structured Query Language,简称SQL，结构化查询语言
    操作关系型数据库的编程语言
    定义操作所有关系型数据库的统一标准

常见的关系型数据库管理系统:
    Oracle:
    MySQL:
    SQL Server:
    PostgreSQL:是一个免费的对象-关系数据库服务器(ORDBMS)，
    DB2:
    SQLite:嵌入式的微型数据库
    MariaDB:MariaDB数据库管理系统是MySQL的一个分支，主要由开源社区在维护

MySQL:
    安装:https://blog.csdn.net/qq_45344586/article/details/129286105
    root@localhost: PH%kMv6O8RK_

    设置my.ini文件:配置文件

    # 初始化mysql
    mysqld --initialize --console

    # 注册mysql服务
    mysqld --install

    启动MySQL服务: net start mysql

    mysqladmin -u root password 1234


    # 登录mysql
    mysql -uroot -p
    mysql -u用户名 -p密码 -h要连接的MySQL服务器的ip地址 -p端口号（3306）

    # alter方式修改
    ALTER USER 'root'@'localhost' IDENTIFIED BY '123456'; //记得修改自己的账户
    flush privileges; //修改成功后刷新权限

    # 退出
    exit
    quit

    Navicat:

    # 卸载
    net stop mysql
    mysql -remove mysql
    删除mysql目录和环境变量就可以了


Navicat:
    ctrl r: 运行查询窗口的所有 sql 语句，即执行所有 sql 语句
    ctrl shift r: 运行查询窗口中选中的 sql 语句，即执行当前选中的 sql 语句