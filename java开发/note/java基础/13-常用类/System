System类：

1. exit 退出当前程序
2. arraycopy：复制数组元素，比较适合底层，一般使用Array.copyOf完成复制数组
    int[] src = {1,2,3};
    int[] dest = new int[3];
    System.arraycopy(src,0,dest,0,3);
    原数组，原数组开始拷贝的位置，目的数组，目标数组拷贝的起始位置，拷贝的元素个数
3. currentTimeMillens:返回当前时间距离1970-1-1的毫秒数
4. gc:运行垃圾回收机制 System.gc()