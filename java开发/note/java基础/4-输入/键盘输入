要想用户输入，需要使用一个扫描器对象，即Scanner
1）导入该类所在的包，Java.util.*
2）创建该类的对象
3）调用里面的方法

Scanner scanner = new Scanner(System.in);
String name = scanner.next();
int age = scanner.nextInt();
double m = scanner.nextDouble();
