1. setName      设置线程名称
2. getName      返回该线程的名称
3. start        线程开始执行
4. run          调用线程对象的run方法
5. setPriority  更改线程的优先级
6. getPriority  获取线程的优先级
7. sleep        线程休眠（毫秒）
8. interrupt    中断线程
    当执行t.interrupt() 就会中断 t线程的休眠
    但并没有真正的结束线程，所以一般用于中断正在休眠的线程。


1. yield    线程的礼让，让出cpu，让其他线程执行，但礼让的时间不确定，所以也不一定礼让成功
2. join     线程的插队。插队的线程一旦插队成功，则肯定先执行完插入的线程所有的任务
