for(循环变量初始化；循环条件；循环遍历迭代){
    循环操作（语句）；
}

while(循环条件){
    循环体；
    循环变量迭代；
}

do{
    循环体；
    循环变量迭代；  
}while(循环条件)；

多重循环：
for(int i=0;i<9;i++)
    for(int j=0;j<9;j++){
}

break;   可以用来跳出循环
continue; 结束本次循环，开始下一次循环