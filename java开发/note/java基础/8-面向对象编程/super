super代表父类的引用，用于访问父类的属性，方法，构造器
可以访问父类的属性和方法（不能访问私有的），以及构造器

当子类中有和父类中成员（属性和方法）重名时，为了访问父类的成员，必须通过
super，this.
不局限于直接父类，基于就近原则。

this表示当前对象，可以调用本类的构造器，必须放在行首，访问本类的方法及属性，没有的话向上查找。
super表示父类对象，可以调用父类的构造器，必须放在行首，访问父类的方法及属性，没有的话向上查找。